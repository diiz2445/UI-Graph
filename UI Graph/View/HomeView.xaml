<UserControl x:Class="UI_Graph.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI_Graph.View"
             xmlns:viewModel="clr-namespace:UI_Graph.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Background="Transparent">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBox VerticalAlignment="Top" HorizontalAlignment="Left" Style="{StaticResource CountTextboxStyle}"/>
                    <Button Content="click me" Height="20" Width="80" HorizontalAlignment="Left" VerticalAlignment="Top" Command=""/>
                </StackPanel>
               
            </Grid>
            
        </Border>
    </Grid>
</UserControl>
